const assert = require('assert');
const matchTransitionFor = require('../../../../../src/world/components/lib/matchTransitionFor');

describe('matchTransitionFor', () => {
  it('', () => {
    assert.deepEqual(matchTransitionFor({

    }), false);
  });
});
