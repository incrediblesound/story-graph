const assert = require('assert');
const getRandomTransition = require('../../../../../src/world/components/lib/getRandomTransition');

describe('getRandomTransition', () => {
  it('', () => {
    assert.deepEqual(getRandomTransition({

    }), false);
  });
});
